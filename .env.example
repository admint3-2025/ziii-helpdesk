# Local dev env - SUPABASE LOCAL
# Copiar este archivo a .env.local y configurar con valores reales
# IMPORTANTE: .env.local NO debe subirse a git (está en .gitignore)

# SUPABASE LOCAL (para desarrollo/pruebas)
NEXT_PUBLIC_SUPABASE_URL=http://10.10.43.10:8000
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-local-anon-key-here
SUPABASE_SERVICE_ROLE_KEY=your-local-service-role-key-here

# SUPABASE CLOUD (para producción)
# NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
# NEXT_PUBLIC_SUPABASE_ANON_KEY=your-cloud-anon-key-here
# SUPABASE_SERVICE_ROLE_KEY=your-cloud-service-role-key-here

# App URL for email links
NEXT_PUBLIC_APP_URL=http://localhost:3000

# SMTP Configuration (server-side only)
SMTP_HOST=smtp.titan.email
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@domain.com
SMTP_PASS=your-password-here
SMTP_FROM="ZIII Helpdesk"
